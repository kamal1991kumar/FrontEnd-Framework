This framework uses [ESDoc](https://esdoc.org/) to create documentation of the project from comments in source code. This documentation is generated in `docs` folder when used `Demo-ui docs` command. Standard boilerplate comments are provided by CLI Tool while creating an entity.